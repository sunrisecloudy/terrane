{
  "name": "renderer-zero",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript reference renderer + patch applier for the Terrane/forge UI wire format (UI-13/14).",
  "type": "module",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "test": "node --test 'test/**/*.test.ts'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "typescript": "5.7.2"
  }
}
