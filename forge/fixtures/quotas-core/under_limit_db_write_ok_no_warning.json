{
  "case": "under_limit_db_write_ok_no_warning",
  "prd": "DL-22",
  "describe": "Under the default (roomy) quota, a real ctx.db.insert via runtime.run is accepted with headroom to spare: the run succeeds and surfaces NO approaching warning (empty quota_warnings, no quota.approaching event). The baseline that proves the warning fires only when a budget is actually approached.",
  "assert": {
    "kind": "db_write",
    "collection": "tasks",
    "seed_runs": [
      { "title": "seed under-limit one" }
    ],
    "set_policy": null,
    "write_run": { "title": "a normal write well under every limit" },
    "expect": {
      "run_ok": true,
      "warns": false,
      "approaching_event_emitted": false
    }
  }
}
