{
  "suite": "live-query-e2e-edge-vectors",
  "version": 1,
  "spec": "../../spec/live-queries.md",
  "prd": ["DL-16", "DL-15", "DL-17", "CR-6", "CR-8"],
  "source": "T047",
  "scope": "End-to-end and edge-case db.watch vectors for transaction rollback, filtered watches, unwatch timing, schema-change behavior, replay, and re-entrant callback mutations.",
  "count": 12,
  "decisions": {
    "no_op_mutations": "patch/update operations dirty their target id even when values are identical; delivery then follows normal filter semantics.",
    "schema_changes": "v1 schema changes are additive-only. Additive watched-collection schema changes do not emit db.watch.notification by themselves; destructive collection drops are rejected before watch invalidation.",
    "reentrant_callbacks": "a notification callback that mutates queues a new event-loop turn after current delivery completes; it never recursively flushes inside the current notification batch."
  },
  "cases": [
    {"case": "rollback_discards_dirty_set_no_notify", "file": "rollback_discards_dirty_set_no_notify.json", "kind": "rollback"},
    {"case": "different_filters_targeted_notifications", "file": "different_filters_targeted_notifications.json", "kind": "filter"},
    {"case": "unwatch_during_pending_batch_suppresses_delivery", "file": "unwatch_during_pending_batch_suppresses_delivery.json", "kind": "unwatch"},
    {"case": "watch_registered_after_mutation_has_no_history", "file": "watch_registered_after_mutation_has_no_history.json", "kind": "registration"},
    {"case": "transact_three_records_two_collections_coalesces", "file": "transact_three_records_two_collections_coalesces.json", "kind": "coalesce"},
    {"case": "filtered_enter_then_leave_same_transaction_no_notify", "file": "filtered_enter_then_leave_same_transaction_no_notify.json", "kind": "filter"},
    {"case": "delete_watched_record_result_excludes_deleted", "file": "delete_watched_record_result_excludes_deleted.json", "kind": "delete"},
    {"case": "schema_change_on_watched_collection_defined_behavior", "file": "schema_change_on_watched_collection_defined_behavior.json", "kind": "schema"},
    {"case": "monotonic_versions_and_shared_transaction_version", "file": "monotonic_versions_and_shared_transaction_version.json", "kind": "version"},
    {"case": "no_op_patch_still_dirties_watched_row", "file": "no_op_patch_still_dirties_watched_row.json", "kind": "noop"},
    {"case": "replay_session_notifications_byte_identical", "file": "replay_session_notifications_byte_identical.json", "kind": "replay"},
    {"case": "reentrant_callback_mutation_queued_next_turn", "file": "reentrant_callback_mutation_queued_next_turn.json", "kind": "reentrant"}
  ]
}
