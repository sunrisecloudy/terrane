{
  "case": "widen_to_nullable_ok",
  "changes": [
    {
      "op": "add_collection",
      "name": "tasks"
    },
    {
      "op": "add_field",
      "collection": "tasks",
      "name": "estimate",
      "ty": "int_num"
    },
    {
      "op": "widen_field",
      "collection": "tasks",
      "field_id": "f0",
      "to": {
        "nullable": "int_num"
      }
    }
  ],
  "expect": "ok",
  "note": "serde shape for FieldType::Nullable is expected to be validated by the Rust fixture adapter"
}
