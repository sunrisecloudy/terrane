{
  "case": "one_file_applet",
  "purpose": "small TS transpile/runtime input",
  "files": [
    {
      "path": "src/main.ts",
      "source": "type Input = { title: string };\nexport async function main(ctx: unknown, input: Input) {\n  return { ok: true, value: { title: input.title.trim() } };\n}\n"
    }
  ]
}
