{
  "version": 1,
  "case": "schema_change_missing_version_rejected",
  "coverage": ["SS-7", "SC-9", "SC-10", "SC-11", "SC-12"],
  "trusted_peer": {
    "actor_id": "actor-maintainer-2",
    "role": "maintainer",
    "db_read": ["*"],
    "db_write": ["*"],
    "schema_write": true
  },
  "incoming_claim": {
    "actor_id": "actor-maintainer-2",
    "role": "maintainer",
    "db_read": ["*"],
    "db_write": ["*"],
    "schema_write": true
  },
  "incoming": {
    "frame": "live_update",
    "doc_id": "schema/tasks",
    "metadata": {
      "resource_type": "schema",
      "op": "schema_change",
      "schema_id": "tasks",
      "compatible": true
    },
    "schema_op": {
      "op": "add_optional_field",
      "schema": "tasks",
      "field": "priority",
      "field_type": "string"
    }
  },
  "expect": {
    "decision": "permission_denied",
    "error_frame": "permission_denied",
    "audit": {
      "action": "sync.schema.change",
      "decision": "deny",
      "reason_contains": "missing schema version in envelope metadata"
    },
    "apply": {
      "import_chunk": false,
      "rebuild_projection": false
    }
  },
  "assertions": [
    "schema-changing ops must name a target schema version before the schema-compatibility gate",
    "an otherwise-authorized maintainer is denied a schema change with no version (fail closed)"
  ]
}
