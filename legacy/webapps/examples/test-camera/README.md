# Test Camera

Bundled example for `resource.invoke` / `resource.read` / `resource.materialize` on the
reference-host bridge (mock camera in dev). Optional **Live preview** uses the browser
camera locally and does not upload frames.

Run from the Terrane launcher at `http://127.0.0.1:7878` → **Test Camera** → **Capture via Terrane**.