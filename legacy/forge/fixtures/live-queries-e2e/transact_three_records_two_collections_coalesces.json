{
  "case": "transact_three_records_two_collections_coalesces",
  "description": "A transact that spans MORE THAN ONE collection is UNSUPPORTED (DL-17, reviews 131/132): it is locally atomic but persists one chunk/oplog row per collection with no transaction-group id, and forge-sync authorizes/applies each chunk independently (SS-7), so a peer denied one collection would import a torn half-transaction. The write boundary REJECTS a cross-collection transact with a typed QueryError before any state changes; NO dirty set / NO notification / no version consumed. The full DL-17 multi-collection-atomic-SYNC (transaction-group metadata + all-or-nothing apply across the SS-7 boundary) is a separate future feature.",
  "unsupported": true,
  "given": {
    "records": [
      {"collection": "tasks", "id": "tasks/1", "fields": {"title": "Existing", "done": false}, "deleted": false},
      {"collection": "notes", "id": "notes/1", "fields": {"body": "Remove me"}, "deleted": false}
    ],
    "watches": [
      {"watch_id": "watch:tasks-all", "query": {"from": "tasks", "orderBy": ["id", "asc"]}},
      {"watch_id": "watch:notes-all", "query": {"from": "notes", "orderBy": ["id", "asc"]}}
    ],
    "next_version": 20
  },
  "when": {
    "mutation": {
      "op": "transact",
      "items": [
        {"op": "patch", "collection": "tasks", "id": "tasks/1", "fields": {"title": "Updated"}},
        {"op": "insert", "collection": "tasks", "id": "tasks/2", "fields": {"title": "New", "done": false}},
        {"op": "delete", "collection": "notes", "id": "notes/1"}
      ]
    }
  },
  "expect": {
    "rejected": true,
    "error_kind": "QueryError",
    "error_contains": "multi-collection transact is not supported",
    "dirty_set": null,
    "notifications": [],
    "version_after": 20,
    "future_feature": "DL-17 multi-collection-atomic-SYNC (transaction-group metadata + all-or-nothing apply across the SS-7 boundary)"
  }
}
