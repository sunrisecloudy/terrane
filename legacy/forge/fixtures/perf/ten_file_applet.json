{
  "case": "ten_file_applet",
  "purpose": "medium TS transpile input",
  "files": [
    {
      "path": "src/lib1.ts",
      "source": "export interface Item1 { id: string; done: boolean; }\nexport function make1(id: string): Item1 { return { id, done: id.length % 2 === 0 }; }\n"
    },
    {
      "path": "src/lib2.ts",
      "source": "export interface Item2 { id: string; done: boolean; }\nexport function make2(id: string): Item2 { return { id, done: id.length % 2 === 0 }; }\n"
    },
    {
      "path": "src/lib3.ts",
      "source": "export interface Item3 { id: string; done: boolean; }\nexport function make3(id: string): Item3 { return { id, done: id.length % 2 === 0 }; }\n"
    },
    {
      "path": "src/lib4.ts",
      "source": "export interface Item4 { id: string; done: boolean; }\nexport function make4(id: string): Item4 { return { id, done: id.length % 2 === 0 }; }\n"
    },
    {
      "path": "src/lib5.ts",
      "source": "export interface Item5 { id: string; done: boolean; }\nexport function make5(id: string): Item5 { return { id, done: id.length % 2 === 0 }; }\n"
    },
    {
      "path": "src/lib6.ts",
      "source": "export interface Item6 { id: string; done: boolean; }\nexport function make6(id: string): Item6 { return { id, done: id.length % 2 === 0 }; }\n"
    },
    {
      "path": "src/lib7.ts",
      "source": "export interface Item7 { id: string; done: boolean; }\nexport function make7(id: string): Item7 { return { id, done: id.length % 2 === 0 }; }\n"
    },
    {
      "path": "src/lib8.ts",
      "source": "export interface Item8 { id: string; done: boolean; }\nexport function make8(id: string): Item8 { return { id, done: id.length % 2 === 0 }; }\n"
    },
    {
      "path": "src/lib9.ts",
      "source": "export interface Item9 { id: string; done: boolean; }\nexport function make9(id: string): Item9 { return { id, done: id.length % 2 === 0 }; }\n"
    },
    {
      "path": "src/main.ts",
      "source": "import { make1 } from \"./lib1\";\nimport { make9 } from \"./lib9\";\nexport async function main(ctx: unknown, input: { id: string }) {\n  return { ok: true, value: [make1(input.id), make9(input.id + \"x\")] };\n}\n"
    }
  ]
}
