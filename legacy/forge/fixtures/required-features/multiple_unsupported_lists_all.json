{
  "case": "multiple_unsupported_lists_all",
  "description": "Several required features are unsupported (one too old, two missing). The refusal must enumerate ALL of them, not just the first.",
  "client_features": [
    { "feature_id": "app", "version": "1.0.0" },
    { "feature_id": "ctx.db.query", "version": "1.0.0" }
  ],
  "manifest_compatibility": {
    "required_features": [
      { "feature_id": "ctx.db.query", "min_version": "2.0.0" },
      { "feature_id": "ctx.future_one", "min_version": "1.0.0" },
      { "feature_id": "ctx.future_two", "min_version": "1.0.0" }
    ]
  },
  "expect": {
    "decision": "refuse",
    "unsupported_feature_ids": ["ctx.db.query", "ctx.future_one", "ctx.future_two"],
    "message_contains": ["ctx.db.query", "ctx.future_one", "ctx.future_two", "3 unsupported"]
  }
}
