{
  "case": "forward_compat_superset_installs",
  "description": "A newer client supports a SUPERSET of features at HIGHER versions than the package requires. Forward compatibility: a higher supported version satisfies a lower required min, and extra client features do not matter, so the install proceeds.",
  "client_features": [
    { "feature_id": "app", "version": "2.4.0" },
    { "feature_id": "ctx.db.query", "version": "2.5.0" },
    { "feature_id": "ctx.files", "version": "1.4.0" },
    { "feature_id": "ctx.extra_future", "version": "3.0.0" }
  ],
  "manifest_compatibility": {
    "min_app_version": "1.0.0",
    "required_features": [
      { "feature_id": "ctx.db.query", "min_version": "2.0.0" },
      { "feature_id": "ctx.files", "min_version": "1.0.0" }
    ]
  },
  "expect": {
    "decision": "install"
  }
}
