{
  "suite": "required-features-negotiation",
  "version": 1,
  "count": 10,
  "source": [
    "prd-merged/08-marketplace-prd.md MP-8 (required_features / capability negotiation), MP-4 (compatibility field)",
    "forge/spec/required-features.md",
    "forge/crates/domain/src/manifest.rs (Compatibility / FeatureRequirement)",
    "forge/crates/core/src/features.rs (ClientFeatureRegistry negotiation)",
    "forge/crates/core/src/signing.rs (signed unknown-field fail-closed gate, reviews 086/089)"
  ],
  "note": "Each case carries a package's compatibility (required_features / min_app_version), the installing client's supported feature set, and the expected install/refuse decision. On refuse, the case pins the ENUMERATED unsupported feature ids and message substrings. The harness drives a real WorkspaceCore::handle(\"applet.install\", ...) with the case's client registry installed, asserting the live install path actually refuses (or proceeds) and names every unsupported feature.",
  "cases": [
    { "case": "all_supported_installs", "file": "all_supported_installs.json" },
    { "case": "one_unsupported_refuses", "file": "one_unsupported_refuses.json" },
    { "case": "min_version_higher_than_client_refuses", "file": "min_version_higher_than_client_refuses.json" },
    { "case": "empty_required_features_installs", "file": "empty_required_features_installs.json" },
    { "case": "signed_policy_field_declared_supported_installs", "file": "signed_policy_field_declared_supported_installs.json" },
    { "case": "signed_policy_field_undeclared_refuses", "file": "signed_policy_field_undeclared_refuses.json" },
    { "case": "multiple_unsupported_lists_all", "file": "multiple_unsupported_lists_all.json" },
    { "case": "feature_id_case_normalization_installs", "file": "feature_id_case_normalization_installs.json" },
    { "case": "forward_compat_superset_installs", "file": "forward_compat_superset_installs.json" },
    { "case": "min_app_version_too_low_refuses", "file": "min_app_version_too_low_refuses.json" }
  ]
}
