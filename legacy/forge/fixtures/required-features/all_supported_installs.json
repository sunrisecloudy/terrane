{
  "case": "all_supported_installs",
  "description": "Every required feature is in the client set at or above the min version, so the install proceeds.",
  "client_features": [
    { "feature_id": "app", "version": "1.0.0" },
    { "feature_id": "ctx.db.query", "version": "1.0.0" },
    { "feature_id": "ctx.files", "version": "1.0.0" }
  ],
  "manifest_compatibility": {
    "required_features": [
      { "feature_id": "ctx.db.query", "min_version": "1.0.0" },
      { "feature_id": "ctx.files", "min_version": "1.0.0" }
    ]
  },
  "expect": {
    "decision": "install"
  }
}
