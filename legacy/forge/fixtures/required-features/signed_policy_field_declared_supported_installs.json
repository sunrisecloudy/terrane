{
  "case": "signed_policy_field_declared_supported_installs",
  "description": "An unknown signed policy field is DECLARED in required_features under the signed.policy.* namespace AND the client supports it, so the negotiation admits it and the install proceeds. This is the accept side that agrees with the signed-package unknown-field fail-closed gate (review 086/089).",
  "client_features": [
    { "feature_id": "app", "version": "1.0.0" },
    { "feature_id": "signed.policy.network_policy", "version": "1.0.0" }
  ],
  "manifest_compatibility": {
    "required_features": [
      { "feature_id": "signed.policy.network_policy", "min_version": "1.0.0" }
    ]
  },
  "expect": {
    "decision": "install"
  }
}
