{
  "case": "empty_required_features_installs",
  "description": "A package that declares no required features and no min app version installs on any client.",
  "client_features": [
    { "feature_id": "app", "version": "1.0.0" }
  ],
  "manifest_compatibility": {
    "required_features": []
  },
  "expect": {
    "decision": "install"
  }
}
