{
  "case": "feature_id_case_normalization_installs",
  "description": "The package's required feature id and the client's registry key differ only by case (and surrounding whitespace). Normalization (trim + ASCII lowercase) is applied symmetrically, so they match and the install proceeds.",
  "client_features": [
    { "feature_id": "app", "version": "1.0.0" },
    { "feature_id": "CTX.DB.Query", "version": "1.0.0" }
  ],
  "manifest_compatibility": {
    "required_features": [
      { "feature_id": "  ctx.db.query  ", "min_version": "1.0.0" }
    ]
  },
  "expect": {
    "decision": "install"
  }
}
