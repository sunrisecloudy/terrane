{
  "case": "redirect_to_unallowlisted_public_denied",
  "allowlist": [
    {
      "method": "GET",
      "url": "https://api.example.com/public/*"
    }
  ],
  "request": {
    "method": "GET",
    "url": "https://api.example.com/public/asset",
    "redirect_chain": [
      "https://api.example.com/public/asset",
      "https://evil.example.net/public/asset"
    ]
  },
  "expect": "deny",
  "reason": "the final redirect hop is a public but non-allowlisted origin; SC-5 re-checks every hop against the allowlist, so the redirect must be denied",
  "requires_runtime_dns": false
}
