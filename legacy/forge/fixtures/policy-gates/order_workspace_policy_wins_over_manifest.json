{
  "version": 1,
  "case": "order_workspace_policy_wins_over_manifest",
  "coverage": ["SC-10"],
  "summary": "ORDER: both the workspace-policy gate (2) AND the resource-allowlist gate (6) would deny — the workspace forbids storage and the key is outside the manifest scope. The engine surfaces the workspace-policy denial because gate 2 runs before the manifest+resource subcheck (first failing gate wins).",
  "actor": { "role": "owner" },
  "manifest": {
    "capabilities": { "storage": { "read": ["app/*"], "write": [] } },
    "limits": { "max_host_calls": 100 }
  },
  "workspace_policy": { "allowed": [], "denied": ["storage"] },
  "run_profile": { "name": "default", "permitted": ["storage"] },
  "platform": { "granted": ["storage"] },
  "request": { "call": "storage", "op": "read", "key": "secret/keys" },
  "expect": {
    "decision": "deny",
    "gate": "workspace-policy",
    "code": "PermissionDenied",
    "reason_contains": "workspace policy",
    "order_note": "workspace-policy (gate 2) wins over the resource-allowlist denial (gate 6)"
  }
}
