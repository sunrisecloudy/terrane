{
  "case": "redacted_secrets_descriptor",
  "export_format_version": 1,
  "expected_exclusions": [
    "secret_plaintext",
    "provider_tokens",
    "local_window_state"
  ],
  "allowed_redactions": [
    {
      "kind": "secret_ref",
      "value": "secret:weather_api",
      "exported_as": "redacted-ref"
    }
  ],
  "reimport_assertions": [
    "import never recreates secret plaintext",
    "applets depending on secret refs require user rebind before running"
  ]
}
