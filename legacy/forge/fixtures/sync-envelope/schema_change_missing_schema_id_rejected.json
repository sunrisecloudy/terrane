{
  "version": 1,
  "case": "schema_change_missing_schema_id_rejected",
  "coverage": ["SS-7"],
  "note": "A schema change naming no schema id is denied fail-closed before the schema-compatibility gate.",
  "envelope": {
    "resource_type": "schema",
    "op": "schema_change",
    "schema_version": 2
  },
  "expect": {
    "well_formed": false,
    "decision": "permission_denied",
    "reason_contains": "missing schema id in envelope metadata"
  }
}
