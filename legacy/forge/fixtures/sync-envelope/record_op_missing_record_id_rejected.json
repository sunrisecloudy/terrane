{
  "version": 1,
  "case": "record_op_missing_record_id_rejected",
  "coverage": ["SS-7"],
  "note": "A record write naming no concrete record id is denied fail-closed; the apply path/audit must name a concrete record (review 092 #2).",
  "envelope": {
    "resource_type": "record",
    "op": "patch",
    "collection": "tasks"
  },
  "expect": {
    "well_formed": false,
    "decision": "permission_denied",
    "reason_contains": "missing record id in envelope metadata"
  }
}
