{
  "version": 1,
  "case": "schema_change_missing_version_rejected",
  "coverage": ["SS-7"],
  "note": "A schema change naming a schema id but no target version is denied fail-closed: the schema-compatibility gate needs a present version (review 092 #2).",
  "envelope": {
    "resource_type": "schema",
    "op": "schema_change",
    "schema_id": "tasks"
  },
  "expect": {
    "well_formed": false,
    "decision": "permission_denied",
    "reason_contains": "missing schema version in envelope metadata"
  }
}
