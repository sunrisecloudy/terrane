{
  "version": 1,
  "case": "record_op_missing_collection_rejected",
  "coverage": ["SS-7"],
  "note": "A record op naming no collection cannot be resource-scoped; denied fail-closed before any grant check even for a trusted owner.",
  "envelope": {
    "resource_type": "record",
    "op": "insert",
    "record_id": "task-1"
  },
  "expect": {
    "well_formed": false,
    "decision": "permission_denied",
    "reason_contains": "missing collection in envelope metadata"
  }
}
