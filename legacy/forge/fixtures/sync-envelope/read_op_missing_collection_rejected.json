{
  "version": 1,
  "case": "read_op_missing_collection_rejected",
  "coverage": ["SS-7"],
  "note": "A read-only catch-up naming no collection cannot be scoped to a db.read grant; denied fail-closed before the grant check.",
  "envelope": {
    "resource_type": "record",
    "op": "read"
  },
  "expect": {
    "well_formed": false,
    "decision": "permission_denied",
    "reason_contains": "read op missing collection in envelope metadata"
  }
}
