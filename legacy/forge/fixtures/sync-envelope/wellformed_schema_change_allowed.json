{
  "version": 1,
  "case": "wellformed_schema_change_allowed",
  "coverage": ["SS-7"],
  "note": "A schema change naming a concrete schema id + target version is well-formed; the trusted owner with schema_write applies it.",
  "envelope": {
    "resource_type": "schema",
    "op": "schema_change",
    "schema_id": "tasks",
    "schema_version": 2
  },
  "expect": {
    "well_formed": true,
    "decision": "allowed"
  }
}
