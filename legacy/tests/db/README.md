# Database Tests

These fixtures define the database test contract for Codex and CI.

The reference host should implement these first using in-memory SQLite. Native hosts and the server should then reuse the same logical contract.
