{
  "name": "app install transaction writes registry records",
  "type": "transaction",
  "steps": [
    "insert apps",
    "insert app_versions",
    "insert app_files",
    "insert app_permissions",
    "insert app_install_reports",
    "activate app"
  ],
  "expected": [
    "active_install_id set",
    "install report persisted",
    "permissions scoped to install id"
  ]
}
