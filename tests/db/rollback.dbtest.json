{
  "name": "rollback restores previous app version",
  "type": "rollback",
  "steps": [
    "install v1",
    "install v2",
    "activate v2",
    "rollback to v1"
  ],
  "expected": [
    "active install is v1",
    "versions immutable",
    "rollback event exists"
  ]
}
