{
  "name": "runtime debug records persist",
  "type": "runtime-logging",
  "steps": [
    "open runtime session",
    "write bridge call",
    "step Zig core",
    "create runtime snapshot",
    "run smoke tests"
  ],
  "expected": [
    "runtime_sessions row exists",
    "bridge_calls row exists",
    "core_events and core_actions rows exist",
    "runtime_snapshots row exists",
    "test_runs row exists"
  ]
}
