Earlier, a previous session was building the app described in the attached
product prompt, but it was interrupted before the app was installed. Work
already done through the local app-building surface may still be saved on the
server as an unfinished draft. Please finish the job: if an unfinished draft of
this app exists, recover the most recent one and complete it — fix anything
broken, validate, and install it — rather than starting over. If no draft
exists, build the app from the product prompt. When finished, the installed app
is the result.
